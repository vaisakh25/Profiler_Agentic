# ── Runtime dependencies ───────────────────────────────────────────────────
pandas==2.3.3
numpy==2.4.2
matplotlib==3.10.8
seaborn==0.13.2
duckdb==1.4.4
fastapi==0.128.8
uvicorn[standard]==0.40.0
psutil>=5.9.0
xlrd>=2.0.1
typing-extensions>=4.15.0

# Parquet engine (Layer 5)
pyarrow==21.0.0

# Encoding detection — optional; validator falls back to UTF-8 if not installed
chardet==5.2.0

# Structured logging (JSON output for production, colored console for dev)
structlog>=24.1.0

# Excel engine (Layer 6)
openpyxl==3.1.5

# Configuration
pyyaml>=6.0
python-dotenv==1.2.1

# MCP server
mcp[cli]==1.26.0

# ── LangGraph agent dependencies ──────────────────────────────────────────

langgraph==1.0.7
langchain-core==1.2.17
langchain-mcp-adapters==0.2.1
langchain-anthropic==1.3.4           # default LLM provider
langchain-community>=0.4.0           # Jina embeddings (API-based)
# Optional: pip install langchain-openai langchain-google-genai
# langchain-openai>=0.1.0
# langchain-google-genai>=1.0.0
# langchain-groq>=0.1.0

# ── pgvector (PostgreSQL vector search) — optional ───────────────────────
# If VECTOR_BACKEND=pgvector or auto (with pgvector extension), uses
# PostgreSQL for vector storage instead of ChromaDB.
pgvector>=0.3.0

# ── Chat persistence (PostgreSQL) — optional ─────────────────────────────
# If POSTGRES_HOST is not set, falls back to in-memory (no persistence).
langgraph-checkpoint-postgres==3.0.5
psycopg[binary,pool]>=3.1.0


# ── Prometheus metrics — optional ────────────────────────────────────────
prometheus_client>=0.20.0 

# ── Development / test dependencies ───────────────────────────────────────

pytest==8.4.2
pytest-cov==7.0.0
pytest-asyncio==1.3.0