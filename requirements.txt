# ── Runtime dependencies ───────────────────────────────────────────────────
pandas==2.3.3
numpy==2.4.2
matplotlib==3.10.8
seaborn==0.13.2
duckdb==1.4.4
fastapi==0.128.8
uvicorn[standard]==0.40.0
psutil==6.1.0
xlrd==2.0.1
typing-extensions>=4.14.0

# Parquet engine (Layer 5)
pyarrow==21.0.0

# Encoding detection — optional; validator falls back to UTF-8 if not installed
chardet==5.2.0

# Excel engine (Layer 6)
openpyxl==3.1.5

# MCP server
mcp[cli]==1.26.0
python-dotenv==1.2.1
boto3==1.35.0

# ── LangGraph agent dependencies ──────────────────────────────────────────

langgraph==1.0.7
langgraph-prebuilt==1.0.7
langchain-core==1.2.17
langchain-mcp-adapters==0.2.1
langsmith>=0.3.13
langchain-anthropic==1.3.4           # default LLM provider
langchain-chroma==1.1.0              # vector store (enrichment)
# Provider libraries (enabled by default for full runtime coverage)
langchain-openai==1.1.7
langchain-google-genai==4.2.1
langchain-groq==1.1.2

# ── Chat persistence (PostgreSQL) — optional ─────────────────────────────
# If POSTGRES_HOST is not set, falls back to in-memory (no persistence).
langgraph-checkpoint-postgres==3.0.5
psycopg[binary,pool]==3.2.3

# ── Development / test dependencies ───────────────────────────────────────

pytest==8.4.2
pytest-cov==7.0.0
pytest-asyncio==1.3.0
