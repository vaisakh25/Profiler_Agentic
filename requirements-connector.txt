# ── Connector MCP Server Dependencies ──────────────────────────────────────
# Lightweight subset for Dockerfile.connector — NO ML/AI stack
# Saves ~2-3GB image size vs full requirements.txt

# Data processing
pandas==2.3.3
numpy==2.4.2
duckdb==1.4.4
pyarrow==21.0.0

# API server
fastapi==0.128.8
uvicorn[standard]==0.40.0

# MCP protocol
mcp[cli]==1.26.0

# Database
psycopg[binary,pool]==3.2.3

# File formats
openpyxl==3.1.5
xlrd==2.0.1
chardet==5.2.0

# Utilities
python-dotenv==1.2.1
psutil==6.1.0
typing-extensions>=4.14.0
